# Agent Context

This folder is generated by `agent-context-kit`.

Use:

- `repo-map.json` for structure
- `summaries/architecture.md` for quick navigation
- `snapshots/latest-runtime.json` for live state if present
